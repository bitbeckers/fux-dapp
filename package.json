{
  "name": "fux-dapp",
  "version": "0.0.1",
  "description": "FUX monorepo",
  "main": "index.js",
  "author": "bitbeckers <code@bitbeckers.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @fux-dapp/contracts run build && pnpm --filter @fux-dapp/graph && pnpm --filter @fux-dapp/frontend run build",
    "test": "pnpm --filter @fux-dapp/** run test"
  }
}
