{
  "name": "fux-dapp",
  "version": "0.0.1",
  "description": "FUX monorepo",
  "main": "index.js",
  "author": "bitbeckers <code@bitbeckers.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "app",
    "contracts",
    "graph",
    "docs"
  ],
  "scripts": {
    "build:fleek": "yarn workspace app build && yarn workspace app export -o ../out/",
    "prestart": "yarn workspace contracts typechain && yarn workspace contracts test",
    "start:graph": "yarn workspace graph deploy:local",
    "start:evm": "yarn workspace contracts local",
    "start:app": "yarn workspace app dev",
    "start": " yarn start:evm & yarn start:app",
    "test": "yarn workspace contracts test"
  }
}
